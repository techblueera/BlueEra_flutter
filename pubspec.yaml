name: BlueEra
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 14.0.4+260

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_svg: ^2.2.3
  octo_image: ^2.1.0
  shimmer: ^3.0.0
  flutter_secure_storage: ^9.2.4
  dio: ^5.8.0+1
  get: ^4.7.2
  flutter_localization: ^0.3.1
  sms_autofill: ^2.4.1
  pinput: ^5.0.1
  image_picker: ^1.2.0
  url_launcher: ^6.3.1
  camera: ^0.10.5+9
  permission_handler: ^12.0.1
  device_info_plus: ^12.1.0
  path_provider: ^2.1.5
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  croppy: ^1.3.6
  path: ^1.9.0
  readmore: ^3.0.0
  visibility_detector: ^0.4.0+2
  geolocator: ^14.0.1
  geocoding: ^4.0.0
  internet_connection_checker_plus: ^2.7.2
  flutter_contacts: ^1.1.9+2
  audioplayers: ^6.5.0
  audio_waveforms: ^2.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  socket_io_client: ^3.1.2
  dotted_border: 2.1.0
#  onesignal_flutter: ^5.3.4
  mime: ^2.0.0
  video_player: ^2.10.0
  file_picker: ^10.2.0
  just_audio: ^0.10.5
  flutter_pdfview: ^1.4.1+1
  pdfx: ^2.9.2
  emoji_picker_flutter: ^4.3.0
  flutter_sound: ^9.28.0
  timeago: ^3.7.1
  razorpay_flutter: ^1.4.1
  webview_flutter: ^4.13.0
  open_file: ^3.5.10
  package_info_plus: ^8.3.0
  share_plus: ^11.1.0
  app_links: ^6.4.1
#  workmanager: ^0.7.0
  loading_animation_widget: ^1.3.0
  flutter_rating_bar: ^4.0.1
  flutter_upgrade_version: ^1.1.8
  flutter_image_compress: ^2.4.0
  any_link_preview: ^3.0.3
  pro_image_editor: ^11.3.0
  carousel_slider: ^5.1.1
  firebase_core: ^4.1.0
  firebase_messaging: ^16.0.1
  firebase_crashlytics: ^5.0.4
  flutter_local_notifications: ^19.4.2
  http_parser: ^4.1.2
  flutter_staggered_grid_view: ^0.7.0
  share_handler: ^0.0.25
  shared_preferences: ^2.5.3
  video_trimmer: ^5.0.0
  get_thumbnail_video: ^0.7.3
  flutter_callkit_incoming: ^3.0.0
  flutter_webrtc: ^1.2.1
  wakelock_plus: ^1.2.1
  flutter_overlay_window: ^0.5.0
  video_compress: ^3.1.4
  syncfusion_flutter_pdfviewer: ^32.1.21
  connectivity_plus: any
  http: any
  translator: ^1.0.4+1
#  pip_view: ^0.9.7
  speech_to_text: ^7.3.0
  google_maps_flutter: ^2.14.2
  percent_indicator: ^4.2.5
  flutter_polyline_points: ^3.1.0
  record: ^6.2.0
  google_generative_ai: ^0.4.7
  envied: ^1.3.3
  qr_flutter: ^4.1.0
  fl_chart: ^0.69.0

  # E2E Encryption
  libsignal_protocol_dart: ^0.8.0   # Signal Protocol keys (Curve25519 generation)
  pinenacl: ^0.6.0                  # NaCl box encryption (matches web tester)
  pointycastle: ^4.0.0              # AES-256-GCM for media encryption
  uuid: ^4.5.3                      # Device ID generation
  convert: ^3.1.2                   # Hex/base64 encoding utilities
  flutter_native_splash: ^2.4.7
  gal: ^2.3.2
  pdf: ^3.12.0
  flutter_cache_manager: any
  google_mobile_ads: ^7.0.0
dependency_overrides:
  intl: ^0.19.0
  # Local patched fork: fixes createConfiguration so iOS can play the device's
  # default ringtone (upstream 3.0.0 always forces a custom ringtoneSound). See
  # packages/flutter_callkit_incoming/ios/Classes/SwiftFlutterCallkitIncomingPlugin.swift
  flutter_callkit_incoming:
    path: packages/flutter_callkit_incoming

flutter_native_splash:
  color: "#ffffff"              # Background color for older Android versions
  image: assets/onboarding/sp_full.png    # Your splash image (logo)

  # Android 12+ Specific Settings (Crucial)
  android_12:
    image: assets/onboarding/sp_full.png # Should be a 1152x1152px image
    color: "#ffffff"                   # Background color
    icon_background_color: "#ffffff"
    # Optional: color behind the icon
  # IMPORTANT: If you re-run `dart run flutter_native_splash:create`, it will
  # overwrite android/app/src/main/res/values-v31/styles.xml and
  # values-night-v31/styles.xml back to the Android 12 icon API
  # (windowSplashScreenAnimatedIcon), which crops the splash to a small
  # centered icon. After regenerating, re-apply the manual fix in those
  # two files (use `windowBackground` -> @drawable/launch_background instead
  # of windowSplashScreenAnimatedIcon).
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator:
  build_runner:
  envied_generator:


flutter:
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/svg/
    - assets/icons/
    - assets/video/
    - assets/sound/
    - assets/category/grocery/menu/
    - assets/category/medical/
    - assets/category/hotel_service/
    - assets/onboarding/individual/
    - assets/onboarding/business/
    - assets/category/foods/
    - assets/category/foods/menu/
    - assets/category/food_home/
    - assets/translations/
    - assets/category/medical/sub_category_medical/


  fonts:
    - family: OpenSans
      fonts:
        - asset: assets/fonts/OpenSans.ttf
          weight: 400
        - asset: assets/fonts/OpenSans-Bold.ttf
          weight: 700
    - family: Arizonia

      fonts:
        - asset: assets/fonts/Arizonia.ttf
    - family: Artifika
      fonts:
        - asset: assets/fonts/Artifika.ttf
    - family: AsapCondensed
      fonts:
        - asset: assets/fonts/AsapCondensed.ttf


