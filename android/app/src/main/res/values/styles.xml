<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- light background so Android will allow black icons -->
        <item name="android:statusBarColor">@android:color/white</item>
        <!-- ask for black icons -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="UCropTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:fitsSystemWindows">true</item>

        <!-- Set your desired status bar color -->
        <item name="android:statusBarColor">#2D93D7</item><!-- Same as your toolbarColor -->
        <item name="android:navigationBarColor">#2D93D7</item>

        <!-- Important: Set this to false to show white icons -->
        <item name="android:windowLightStatusBar">true</item> <!-- true if using light status bar -->
        <item name="android:windowLightNavigationBar">false</item>

    </style>

    <style name="CircularImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>
