class CollapsibleGridModel {
  final String name;
  final String slugId;
  final String? icon;

  const CollapsibleGridModel({
     required this.name,
     required this.slugId,
     this.icon,
  });
}