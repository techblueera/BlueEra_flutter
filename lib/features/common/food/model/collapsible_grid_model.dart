class CollapsibleGridModel {
  final String name;
  final String icon;
  final String slugId;

  const CollapsibleGridModel({
     required this.name,
     required this.icon,
     required this.slugId
  });
}