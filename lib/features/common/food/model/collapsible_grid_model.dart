class CollapsibleGridModel {
  final String label;
  final String icon;
  final String tagId;

  const CollapsibleGridModel({
     required this.label,
     required this.icon,
     required this.tagId
  });
}