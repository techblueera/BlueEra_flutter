

> **⚠ PARTLY SUPERSEDED.** Written against the v1/v2 guide. Its request/response
> shapes were revised by v3: the role is `actor` (not `isOwner`), money is
> `data.payment.*` (not `paymentSummary`), `warning` is a sibling of `data`, and
> `delivery` must carry `location.coordinates` as `[lng, lat]`. **Current state:
> `ORDER_FLOW_V3_FRONTEND_DONE.md` (same folder).**